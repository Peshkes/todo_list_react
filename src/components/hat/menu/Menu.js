import './Menu.css';
import React, {Component} from 'react';

class Menu extends Component {
    render() {
        return (
            <nav className={'menu'}>
                <button className={'menuBtn'}/>
            </nav>
        );
    }
}

export default Menu;